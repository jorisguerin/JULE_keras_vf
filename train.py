from JULE import JULE

### Load parameters ###
from parameters import * 
#load all the informations about 
	#the dataset,
	#the model,
	#the training process,

#Load original features and true labels
#Initialize clusters
jule = JULE(datafile, modelfile, jule_params)